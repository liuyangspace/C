/*

union

联合体: 保存不同类型和长度对象的变量。

定义:
    union unionName {
        strict {
            int type;
        };
        int ival;
        float fval;
    }
声明:
    union unionName{...} u;
    union unionName u;
初始化:

使用:
    联合名.成员
    联合名->成员

*/
