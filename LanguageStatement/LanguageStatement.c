/*
C
    形式：
        1，每个程序从 main 函数开始运行
        2，所有变量先声明后使用

    注释：

    运算符：

    流程控制：
        while(boolean)
        for(init;boolean;handle)

        顺序，分支，循环，跳转


    指令：
        #include    引入文件
            #include <headFileName.h>
        #define     定义符号常量
            #define constantName value


*/
#include <stdio.h> /* */
