
C

1,词法
    注释： /* ... */ 多行注释(不能嵌套)， // 单行注释
    标识符：由字母(区分大小写)、下划线、数字组成，首字符为字母或下划线
        常量：
    关键字：
        存储类别:
            auto
            register
            extern
            static
        数据类型：
            void
            unsigned
            signed
            short
            long
            int
            float
            double
            char
        结构定义：
            const
            struct
            union
            enum
            typedef
        流程控制：
            goto
            if
            else
            switch
            case
            default
            for
            while
            do
            continue
            break
            return

            sizeof
            volatile


